# tbbstock 自動登入 — 簡易說明

完整步驟、疑難排解看 `詳細說明.md`。

## 開啟指令視窗

檔案總管進到這個資料夾 → 空白處按右鍵 → **「在終端機中開啟」**。

## 首次設定（每台電腦做一次）

**1. 填帳密**

用記事本打開 `.env`，把 `TBB_ID_1` / `TBB_PASSWORD_1` 改成真的身分證和密碼。
其他設定不用動。

**2. 建立專用的瀏覽器資料夾**

指令前面的 `.\` 不能省略。

```powershell
.\setup-profile.ps1
```

會開一個 Chrome 視窗 —— **直接關掉，不用登入 Google**。

**3. 找出憑證在哪**

```powershell
.\migrate-cert.ps1
```

看輸出裡標示 `[有憑證]` 的那一行，記下它下面的路徑。
全部都是 `[  --  ]` 的話先停下來，看 `詳細說明.md` 的〈附錄 B〉。

**4. 關掉所有 Chrome，再複製憑證**

```powershell
.\migrate-cert.ps1 -From "第 3 步記下的路徑"
```

**5. 驗證**

```powershell
.\tbb-login.exe
```

沒跳出橘色的「瀏覽器查無有效數位憑證」就完成了。

## 使用自動化登入

雙擊 `tbb-login.exe` 就好。

你日常那個 Chrome 不受影響，照常用。

## 兩件不要做的事

1. **不要在自動登入的瀏覽器裡按「憑證申請」** —— 你日常 Chrome 那張會失效
2. **不要把 `chrome-profile` 資料夾複製到別台電腦** —— 不會生效，每台都要重做首次設定
