# auto-login / 持股同步

自動登入某券商網站（tbbstock）+ Playwright 抓即時資料 + COM 寫回 Excel「持股管理」檔。
Tkinter GUI 是唯一的操作介面（雙擊 `tbb-login.exe`），命令列模式已整支拿掉。

Excel 版面完全不動（公式、巨集原地保留），程式只認得 B8（現金）、D/E/F 欄（股數／成本）
這三處，其餘資訊靠旁邊兩個檔案自己記帳。**這個「Excel 是唯一畫面、程式只覆蓋固定幾格」
的定位是刻意的**，別為了方便改成程式自己畫一張持股表——那是 [[介面規劃]] 第十七節
在規劃的另一個方向，還沒定案動手（見下方「進行中／未定案」）。

## 資料放哪裡

| 檔案 | 內容 | 誰維護 |
| --- | --- | --- |
| `持股管理-*.xls` | B8 現金餘額、E/F 欄股數與成本，其餘全是原本的公式 | 人＋程式 |
| `*-同步紀錄.json` | 現金基準（今日初始現金餘額）、上次程式寫入的值、`settings` | 程式（`ledger.py`） |
| `*-同步歷程.jsonl` | 一行一筆異動，只增不改，「歷程」分頁與同步分頁的訊息框都讀這份 | 程式（`ledger.py`） |
| `.env` | 帳密、模擬帳號數、UI 尺寸、`CASH_METHOD_TOGGLE` 等開關 | 人 |

兩個新檔案的檔名跟著 Excel 檔名走（不是固定名稱），放在 Excel 旁邊同一個資料夾。
紀錄檔掉了不會壞事——程式會判定「所有格子都不認得」，安全地整格不寫。

## 模組地圖

| 檔案 | 職責 |
| --- | --- |
| `login.py` | 登入、開瀏覽器、cookie store（換人＝換 cookie，不是重登） |
| `fetch.py` | 抓網頁資料（`collect()`），登入完立刻抓完那一組才換下一組 |
| `recon.py` | AJAX 重放，只讀不寫，偵察新查詢用（`python recon.py 1`） |
| `planner.py` | 網頁資料 × Excel 現值 × 紀錄檔 → 一張「變更提案」清單，純計算 |
| `ledger.py` | 紀錄檔讀寫、現金基準、歷程追加 |
| `excel_io.py` | COM 開檔、讀寫 B8/E/F，只認得這三處（2026/08/24 起不再自動備份） |
| `util.py` | 數字與寬度對齊等小工具 |
| `ui.py` / `ui_layout.py` / `ui_sync.py` / `ui_background.py` / `ui_common.py` / `ui_history.py` / `ui_cert.py` | Tkinter GUI，唯一有畫面的一批檔案；背景執行緒跑 Playwright／COM，主執行緒才碰 widget |
| `dev_tools/simulate.py` | 假帳號、假網頁（`window.__SIM__`），讓 `fetch.py` 走假資料但形狀跟真 API 一樣 |
| `dev_tools/sim_excel.py` | 在 Excel 裡加/移除模擬分頁 |
| `dev_tools/check_two_accounts.py` | 多帳號「各拿各的資料」回歸測試 |

## 改動前必看：已定案、別擅自改的規則

現金餘額算法、B8 讀寫策略是這個專案裡踩過最多坑、也最容易被「順手優化」改壞的部分。
細節都在 [[現金餘額兩種算法]]，這裡只列改代碼前一定要知道的結論：

- **現金餘額兩種算法並存，一個總開關（20 人一起），不是一人一格**：
  `方法一` = 今日初始現金餘額 + 今日淨收付（累加）；`方法二` = 銀行餘額 + 還沒交割的淨收付
  （快照，用 `CMD=query610` 交割金額查詢）。兩者都是「對」的，只是對的日子不同
  （全額交割股那天方法二會扣兩次錢）。
- **現金是「每天重新起算」，不是逐日累加**：今天第一次登入時的 B8 直接當今日起點，
  不回頭補漏跑的日子。
- **B8 無條件覆蓋**，不像股數/成本那樣比對 `last_written` 才決定寫不寫——它寫的是公式，
  數字剛好沒變也要覆蓋，否則畫面上的公式會是過期的。
- **2026/08/20 起，股數／成本／現金全部一律覆蓋 Excel，沒有自動/手動偵測、沒有「接管」
  這個概念了**（見 [[拿掉每格自動偵測]]）。`介面規劃.md` 裡任何提到「手動格子」「交還給
  程式」的段落是歷史決策，不是現狀。
- **`branchId` 查詢要加 `'1'` 前綴**（`query610`、`queryBankBalance`），但 `transDateQuery`
  **不加**。錯了不會報錯，只是靜靜少算或算出 0——這條最危險，改到這幾支查詢務必對照
  `docs/現金餘額兩種算法.md` 裡「網頁那邊怎麼拿」那節重新核對一次。
- **`queryBankBalance` 的 `Amount` 單位是分，要除以 100**。
- **一次讀取只動「這一輪的範圍」（`round_scope`）**：只更新一位交易人時，寫入／落帳／
  接管都只能碰他，不能因為讀了新資料就順手把別人上一輪的舊提案也寫進 Excel。
- **`.bat` 檔案內容只能是純 ASCII**（連中文註解都不行）：cmd.exe 用系統 OEM 編碼解析
  `.bat`，混中文會被誤判成指令去執行。呼叫同層 exe 一律用 `%~dp0` 完整路徑。
- exe 打包成 `--windowed`（雙擊直接開 GUI，沒有黑視窗），啟動失敗時看 exe 旁邊的
  `crash.log`，不會有主控台輸出可看。

## 進行中／未定案（下一次改動前先確認狀態）

- **把 Excel 搬進來，Excel 降成存檔**（[[介面規劃]] 第十七節，2026/08/19 定方向，
  **還沒動手**）：長期規劃是把「更新股價／填比重／自動計算／下單試算」四個 Excel 上
  的日常動作全部搬進程式，Excel 降成單純的存檔格式。屬於大改動，動手前這個方向要先
  跟使用者重新確認還算不算數。
- **真帳號目前只有 1 組，20 組要陸續部署**：多帳號相關的程式邏輯（cookie 換人、
  round_scope、身分核對）已經照 20 組的規模寫，但「換人不重登」「session 多久逾時」
  這類行為只在模擬環境測過，沒有在真實 20 組帳號上跑過。

## 驗證方式

抓真網頁資料的登入+讀取約需 7~8 秒／組（不是 UI 卡頓，是真的在等瀏覽器）。日常改動：

1. 先用 `dev_tools/simulate.py`（`.env` 設 `SIMULATE_ACCOUNTS=19`）+
   `dev_tools/sim_excel.py --write` 跑模擬帳號，確認邏輯對。
2. 自動化測試腳本不能有互動等待（`按 Enter 才繼續`那種），要用 `echo | python ...`
   餵空輸入——Windows 上 `/dev/null` 擋不住 tty 的等待。
3. 邏輯確認沒問題後，才用這台機器上已有的真憑證跑一次真登入驗證。

## 部署

`dist\` 資料夾複製到目標機器即可執行，不需要重新打包。目標機器要跑之前，需要
`chrome-profile`（不能跨機器複製，每台要重做 `setup-profile.ps1` + `migrate-cert.ps1`）。
