# 把這個檔案複製成 .env，填入自己的帳密後即可使用。
# .env 要跟 login.py（或打包後的 tbb-login.exe）放在同一層資料夾。

# ---- 帳號 ----
# 可設定多組，依序編號 _1、_2、_3...，程式會每組各開一個分頁自動登入。
TBB_ID_1=your_id_number_1
TBB_PASSWORD_1=your_password_1

TBB_ID_2=your_id_number_2
TBB_PASSWORD_2=your_password_2

# ---- 要同步哪一份 Excel ----

# 持股管理表的位置。相對路徑以這個 .env 所在的資料夾為基準。
# 沒設的話程式不會猜任何預設檔名 —— 開介面按「載入EXCEL」選一次，
# 程式就會自動把選好的路徑寫回這一行，下次開啟直接用。
# EXCEL_PATH=dist\持股管理-台美股-5家.xls

# ---- 介面 ----

# 以下三項改完都要重開介面才生效，填了看不懂的東西一律當作沒設，不會讓介面開不起來。

# 介面字級：按鈕、表格、對話框的文字大小。留空或不設就是 12。
# 路徑、提醒那類次要文字會小一級，欄寬與列高也會跟著一起放大，都不必另外調。
# 只吃 8～24 的整數，超出範圍會夾到邊界。
# UI_FONT_SIZE=12

# 視窗開起來的長寬，單位是像素（填多少就是多少，不會再乘上字級）。
# 不設的話用預設值，會跟著字級一起放大（12 級時約 1180x720），通常比填死一個
# 數字好，所以預設兩行都不設。帳號多到左邊名單放不下時是出現捲軸、不會把視窗
# 撐高 —— 想一次看完整份名單就在這裡把高度設大一點。
# 太小的值會被拉到 640x400，太大的值會被縮到桌面可用區（螢幕扣掉工作列）以內。
# UI_WIDTH=1180
# UI_HEIGHT=720

# ---- 登入 ----

# 攔截到驗證碼後、以及按下登入前，各要多等幾毫秒。留空或不設就是 200。
# 頁面載入時可能會再打一次 VerifyNumberServlet 換掉驗證碼，這段等待就是在等它安定：
# 太短會填到已經失效的舊驗證碼（畫面上看起來像是密碼打錯），太長則每個帳號都多等這麼久。
# 網路慢、常常登入失敗時就往上加（例如 500）。
# 只吃 0～5000 的整數，超出範圍會夾到邊界，填了看不懂的東西一律當作沒設。
# VERIFY_SETTLE_MS=200

# ---- 瀏覽器 ----

# 用哪個瀏覽器：chrome、msedge...。留空則用 Playwright 自己下載的 Chromium
# （第一次執行會自動下載約 150MB）。
BROWSER_CHANNEL=chrome

# 使用者資料夾，tbbstock 的數位憑證與 Cookie 存在這裡；留空則每次都開全新的空白 profile
# （憑證也就存不住，登入後會被「瀏覽器查無有效數位憑證」擋下來）。
# 相對路徑以本檔案所在資料夾為基準。
#
# 設好之後執行一次 setup-profile.ps1 把資料夾建出來（視窗直接關掉，不用登入任何帳號），
# 再用 migrate-cert.ps1 把憑證複製進來。
# 每台電腦都要各做一次 —— 這個資料夾不能從別台電腦複製過來，Chrome 的加密會讓它失效。
# 也不能指向 %LOCALAPPDATA%\Google\Chrome\User Data，Chrome 會拒絕自動化連線。
USER_DATA_DIR=chrome-profile

# 上面資料夾底下要用哪個設定檔（Default、Profile 1...），留空就是 Default。
# 只有在同一個資料夾裡分了多個 Chrome 使用者才需要設。
# BROWSER_PROFILE_DIR=Default

# 直接指定瀏覽器執行檔路徑（會蓋過 BROWSER_CHANNEL），一般不用設。
# BROWSER_PATH=C:\Program Files\Google\Chrome\Application\chrome.exe

# ---- 測試用：模擬多帳號 ----

# 只有一組真帳號也能測「20 個帳號」的流程：在真帳號後面接上幾個假帳號
# （交易人A、交易人B...）。假帳號不連任何網站，只在同一個瀏覽器多開一個分頁，
# 放一頁程式自己畫的假網頁，數字可以在頁面上直接改（見 dev_tools/simulate.py）。
# 正式使用時不要設這一行 —— 沒設就等於整套模擬不存在。
# 設了之後要先用 dev_tools/sim_excel.py 在 Excel 補上對應的分頁。
# SIMULATE_ACCOUNTS=19
